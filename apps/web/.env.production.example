VITE_MAPTILER_KEY=your_browser_key

# Same-origin: browser calls /api/… and FastAPI strips the /api prefix.
VITE_API_URL=/api

# Must match ADMIN_PATH on the API (no leading slash). Leave blank to hide ops UI.
VITE_OPS_PATH=ridge-ops-7f3k9q
